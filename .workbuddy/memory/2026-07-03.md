# 2026-07-03 工作日志

## 分析了 admin-pj 仓库登录认证体系

- 深入分析了 6 个核心文件: login/index.vue, api/user, store/modules/user, utils/auth.ts, router/index.ts, http/axios
- 完整梳理了登录 →token 存储 → 路由守卫 → 退出登录的全链路
- 发现: 无验证码/记住密码/SSO，token 存 localStorage(key=fast-token)，退出用 location.reload()
- 项目基于 fast-vue3 模板: Vue3 + Vite2 + TS + Pinia + Element Plus
- 默认账号: adm/123 (admin), test/test (user)，mock 提供

## 全面冗余排查与清理

- 审查了全部源文件 + 配置文件 + packages/ + mock/ + plop-tpls/
- 发现 6 大类冗余: packages/示例项目、无用依赖包、垃圾页面代码、冗余 Vite 插件、残留配置、代码风格问题

### 已执行清理（3 次提交）

1. `f11067c` - chore: 清理项目冗余代码和无用依赖

   - 删除 packages/ 目录（3 个无关子项目，~80% 仓库体积）
   - 删除 plop-tpls/、plopfile.js、yarn.lock
   - package.json: 移除 @vueuse/core、@vueuse/components、mockjs、plop、import、postcss-px-to-viewport
   - 重写 src/pages/home/index.vue（删除 280 行算法练习代码）
   - 删除 src/pages/dashboard/、src/pages/features/table2/、src/pages/index.vue
   - 修复 stylelint.config.js（删除错误 ignoreFiles）
   - pnpm-workspace.yaml: 删除 packages/\* 配置

2. `91d7143` - fix: 修复 .gitignore 配置

   - 删除 pnpm-lock.yaml\*（不应忽略 lock 文件）
   - 修复 ./packages 写法 → packages/
   - 添加 .workbuddy/、.pnpm-store/、public/rawgraphs/ 到忽略

3. `63e3b3e` - chore: 删除 vite-plugin-mock 相关配置
   - config/vite/plugins/index.ts: 删除 ConfigMockPlugin import 和 push
   - 删除 config/vite/plugins/mock.ts

## 登录相关记忆

- 登录页面: src/pages/login/index.vue (username + password 表单)
- API: src/api/user/index.ts (POST /user/login, GET /user/profile, POST /user/logout)
- Store: src/store/modules/user/index.ts (Pinia, login/logout/getInfo actions)
- Token 管理: src/utils/auth.ts (localStorage key='fast-token', value='Bearer xxx')
- 路由守卫: src/router/index.ts (beforeEach, isLogin() 检查 token)
- HTTP 封装: src/utils/http/axios/index.ts (统一响应 IResponse{code,result,message})
